#include <stdio.h>
/**
 * main - Entry point
 *
 * Description - Niggercode
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int tic, toc;

	for (tic = '0'; tic <= '9'; tic++)
	{
		putchar(tic);
	}
	for (toc = 'a'; toc <= 'f'; toc++)
	{
		putchar(toc);
	}
	putchar('\n');
	return (0);
}
