#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Prints'
 * @param void
 *
 * Return: Always 0 (Success)
 */
int main(void)
{char *str1 = "''Programmings is like building a multilingual puzzle";
puts(str1);
return (0);
}
